name: Auto-generate CHANGELOG.md

on:
  push:
    branches:
      - main
    paths:
      - 'ui/**'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  changelog:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'

      - name: Detect changed folders and generate changelogs
        run: node .github/scripts/generate-changelog.js

      - name: Commit and push changes
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          
          if [[ -n $(git status -s) ]]; then
            git add ui/*/CHANGELOG.md 2>/dev/null || true
            git commit -m "docs: auto-generate CHANGELOG.md for changed components" || true
            git push origin main
          else
            echo "No changes detected"
          fi

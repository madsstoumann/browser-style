{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "id": "card",
  "title": "🃏 Card",
  "description": "A universal, schema.org-driven card. One structured envelope (headline, summary, media, authors, actions, engagement…) shared by every card, a `schemaType` discriminator covering 25 schema.org types, and a single `details` object for type-specific data. Presentation (variant/media/content tokens for <ui-card>/<ui-reveal>) deliberately lives OUTSIDE this model — in a referenced card-preset — so the same card can render as hero, grid item, overlay or flip by swapping one reference.",
  "metadata": {
    "localized": true,
    "version": "1.3.3",
    "lastModified": "2026-07-06",
    "displayField": "internalName"
  },
  "structure": {
    "allowedAsRoot": true,
    "folder": "Cards",
    "layout": [
      {
        "area": "main",
        "type": "group",
        "name": "Basics",
        "description": "Type and primary text content",
        "collapse": false,
        "fields": ["internalName", "schemaType", "eyebrow", "headline", "subheadline", "summary"]
      },
      {
        "area": "main",
        "type": "group",
        "name": "Body & Dates",
        "description": "Long-form text and publication dates",
        "collapse": true,
        "fields": ["body", "published", "modified", "readingTime"]
      },
      {
        "area": "main",
        "type": "group",
        "name": "Media",
        "description": "Media items (more than one becomes a carousel) and overlay furniture",
        "collapse": true,
        "fields": ["media", "furniture"]
      },
      {
        "area": "main",
        "type": "group",
        "name": "People & Engagement",
        "description": "Authors and interaction statistics",
        "collapse": true,
        "fields": ["authors", "engagement"]
      },
      {
        "area": "main",
        "type": "group",
        "name": "Navigation",
        "description": "Tags, call-to-action buttons and related links",
        "collapse": true,
        "fields": ["tags", "actions", "links"]
      },
      {
        "area": "main",
        "type": "group",
        "name": "Type Details",
        "description": "Schema-type-specific data, the look & feel preset, and the optional reveal flipside",
        "collapse": false,
        "fields": ["details", "preset", "flipside"]
      }
    ]
  },
  "type": "object",
  "properties": {
    "internalName": {
      "type": "string",
      "title": "Internal Name",
      "description": "Internal name for CMS editors — not displayed on the front end",
      "localized": false,
      "validation": {
        "required": true
      }
    },
    "schemaType": {
      "type": "select",
      "title": "Schema Type",
      "description": "schema.org content type. Drives the emitted itemtype, the microdata property mapping (headline → headline/name/title, summary → description/reviewBody) and which `details` shape applies.",
      "localized": false,
      "default": "content",
      "validation": {
        "required": true
      },
      "options": [
        { "value": "content", "label": "Content (CreativeWork — fallback)" },
        { "value": "article", "label": "Article" },
        { "value": "news", "label": "News (NewsArticle)" },
        { "value": "product", "label": "Product" },
        { "value": "event", "label": "Event" },
        { "value": "recipe", "label": "Recipe" },
        { "value": "review", "label": "Review" },
        { "value": "job", "label": "Job (JobPosting)" },
        { "value": "course", "label": "Course" },
        { "value": "booking", "label": "Booking (Reservation)" },
        { "value": "poll", "label": "Poll (Question)" },
        { "value": "profile", "label": "Profile (Person)" },
        { "value": "faq", "label": "FAQ (FAQPage)" },
        { "value": "quote", "label": "Quote (Quotation)" },
        { "value": "timeline", "label": "Timeline (EventSeries)" },
        { "value": "gallery", "label": "Gallery (ImageGallery)" },
        { "value": "statistic", "label": "Statistic (Observation)" },
        { "value": "achievement", "label": "Achievement (EducationalOccupationalCredential)" },
        { "value": "announcement", "label": "Announcement (SpecialAnnouncement)" },
        { "value": "business", "label": "Business (LocalBusiness)" },
        { "value": "comparison", "label": "Comparison (ItemList)" },
        { "value": "contact", "label": "Contact (ContactPoint)" },
        { "value": "location", "label": "Location (Place)" },
        { "value": "membership", "label": "Membership (Offer)" },
        { "value": "social", "label": "Social (SocialMediaPosting)" },
        { "value": "software", "label": "Software (SoftwareApplication)" },
        { "value": "organization", "label": "Organization (multi-office)" },
        { "value": "video", "label": "Video (VideoObject)" },
        { "value": "howto", "label": "How-to (HowTo)" },
        { "value": "qa", "label": "Q&A (QAPage)" },
        { "value": "podcast", "label": "Podcast (PodcastEpisode)" },
        { "value": "movie", "label": "Movie" },
        { "value": "book", "label": "Book" },
        { "value": "dataset", "label": "Dataset" },
        { "value": "claim", "label": "Fact check (ClaimReview)" }
      ]
    },
    "eyebrow": {
      "type": "string",
      "title": "Eyebrow",
      "description": "Category / kicker shown above the headline. Emitted as articleSection, category, recipeCategory or about depending on schemaType."
    },
    "headline": {
      "type": "richtext",
      "title": "Headline",
      "description": "Primary headline. Emitted as headline (article/news), title (job) or name (all other types). Short rich text: inline <b> (emphasis) and <ui-gradient-text> (gradient treatment, optional animate slide|breathe) pass through; all other markup is stripped/escaped. Keep ≤256 chars — if the CMS cannot constrain rich text length, fall back to a plain rich text field. Plain strings remain valid.",
      "validation": {
        "required": true,
        "maxLength": 256
      },
      "options": {
        "allowImages": false,
        "allowEmbeds": false
      }
    },
    "subheadline": {
      "type": "string",
      "title": "Subheadline",
      "description": "Secondary headline / tagline"
    },
    "summary": {
      "type": "text",
      "title": "Summary",
      "description": "Brief summary. Emitted as description — or reviewBody (review), text (quote/social)."
    },
    "body": {
      "type": "richtext",
      "title": "Body",
      "description": "Optional long-form content (legacy content.text paragraphs)",
      "options": {
        "allowImages": false,
        "allowEmbeds": false
      }
    },
    "published": {
      "type": "datetime",
      "title": "Published",
      "description": "Publication date/time → datePublished / datePosted"
    },
    "modified": {
      "type": "datetime",
      "title": "Modified",
      "description": "Last modified date/time → dateModified"
    },
    "readingTime": {
      "type": "string",
      "title": "Reading Time",
      "description": "Human-readable reading time, e.g. “5 min read”"
    },
    "media": {
      "type": "array",
      "title": "Media",
      "description": "Media items rendered inside <ui-media>. One item = single frame; multiple items = carousel (nav dots/arrows).",
      "items": {
        "type": "object",
        "properties": {
          "asset": {
            "type": "media",
            "title": "Asset",
            "description": "Image or video asset from the media library",
            "options": {
              "accept": ["image/*", "video/*"],
              "focalPoint": true
            }
          },
          "mediaType": {
            "type": "select",
            "title": "Media Type",
            "localized": false,
            "default": "image",
            "options": [
              { "value": "image", "label": "Image" },
              { "value": "video", "label": "Video" },
              { "value": "youtube", "label": "YouTube" },
              { "value": "vimeo", "label": "Vimeo" },
              { "value": "audio", "label": "Audio" }
            ]
          },
          "src": {
            "type": "url",
            "title": "External Source",
            "description": "External URL or provider video id — used when no asset is set (e.g. YouTube/Vimeo id)"
          },
          "alt": {
            "type": "string",
            "title": "Alt Text",
            "description": "Accessible description of the media item"
          },
          "caption": {
            "type": "string",
            "title": "Caption",
            "description": "Optional visible caption (data-part=\"caption\")"
          },
          "uploadDate": {
            "type": "datetime",
            "title": "Upload Date",
            "description": "Video only → VideoObject uploadDate"
          },
          "duration": {
            "type": "string",
            "title": "Duration",
            "description": "Video only, ISO 8601 (e.g. PT2M10S) → VideoObject duration"
          },
          "description": {
            "type": "string",
            "title": "Description",
            "description": "Video only → VideoObject description"
          }
        }
      }
    },
    "authors": {
      "type": "array",
      "title": "Authors",
      "description": "Authors / reviewers / speakers — rendered as byline, emitted as author → Person",
      "items": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "title": "Name",
            "validation": {
              "required": true
            }
          },
          "role": {
            "type": "string",
            "title": "Role",
            "description": "Job title or role → jobTitle"
          },
          "avatar": {
            "type": "media",
            "title": "Avatar",
            "options": {
              "accept": ["image/*"]
            }
          }
        }
      }
    },
    "tags": {
      "type": "tags",
      "title": "Tags",
      "description": "Keywords — rendered as pill list (data-part=\"tags\"), emitted as keywords"
    },
    "actions": {
      "type": "array",
      "title": "Actions",
      "description": "Call-to-action buttons (data-part=\"actions\")",
      "items": {
        "type": "object",
        "properties": {
          "link": {
            "type": "link",
            "title": "Link",
            "options": {
              "allowInternal": true,
              "allowExternal": true
            }
          },
          "style": {
            "type": "select",
            "title": "Style",
            "localized": false,
            "default": "primary",
            "options": [
              { "value": "primary", "label": "Primary" },
              { "value": "secondary", "label": "Secondary" },
              { "value": "ghost", "label": "Ghost" }
            ]
          }
        }
      }
    },
    "links": {
      "type": "array",
      "title": "Links",
      "description": "Plain related links (rendered as text links, not buttons)",
      "items": {
        "type": "link",
        "title": "Link",
        "options": {
          "allowInternal": true,
          "allowExternal": true
        }
      }
    },
    "furniture": {
      "type": "object",
      "title": "Furniture",
      "description": "Overlay elements placed on top of the media area: a chip label, a beacon (live/status indicator), a sticker callout, a save/favorite toggle and a play affordance. This model carries only their TEXT/semantic content — position, hue, size and shape are set in the preset's media= token string (chip/sticker/save/play/beacon tokens), optionally overridden per item via 'style'.",
      "properties": {
        "chip": {
          "type": "object",
          "title": "Chip",
          "description": "Small pill label overlaid on the media (<ui-chip>) — replaces the legacy ribbon.",
          "properties": {
            "text": {
              "type": "string",
              "title": "Text"
            },
            "badge": {
              "type": "string",
              "title": "Badge",
              "description": "Optional count/label rendered as a nested <ui-badge> (e.g. \"4\")."
            },
            "style": {
              "type": "string",
              "title": "Style override",
              "localized": false,
              "description": "Extra chip() tokens appended after the preset, e.g. \"red\" or \"bs red\". Overrides the preset for the same axis (position/hue)."
            }
          }
        },
        "beacon": {
          "type": "object",
          "title": "Beacon",
          "description": "Live/status indicator overlaid on the media (<ui-beacon>) — the animated counterpart to the static chip (e.g. LIVE, REC, Breaking). Marker-class: rendered text-only. Animations are gated behind prefers-reduced-motion: no-preference; the paused attribute freezes a running one.",
          "properties": {
            "text": {
              "type": "string",
              "title": "Text"
            },
            "style": {
              "type": "string",
              "title": "Style override",
              "localized": false,
              "description": "Extra beacon() tokens appended after the preset, e.g. \"red sld bln\" or \"ts pll\". Axes: position (ts…be) · hue (red…white) · face (dts dots | pll pill | sld solid | tck ticker) · animation (bln blink | pls pulse | brt breathe | non none) · size (xs sm md lg xl 2xl) · corner (rnd sqr). Overrides the preset for the same axis."
            }
          }
        },
        "sticker": {
          "type": "object",
          "title": "Sticker",
          "description": "Callout disc/shape overlaid on the media (<ui-sticker>). Its multi-line typography is authored as 'lines' — each line renders as its own element and is styled by the sticker's font= / font-lead= tokens.",
          "properties": {
            "lines": {
              "type": "array",
              "title": "Lines",
              "description": "One entry per typographic line. 'label' → <small> (font=), 'lead' → <strong> (font-lead=), 'plain' → <span> (fluid). Optional 'sup' renders a trailing <sup> (e.g. price cents).",
              "items": {
                "type": "object",
                "properties": {
                  "role": {
                    "type": "select",
                    "title": "Role",
                    "localized": false,
                    "default": "label",
                    "options": [
                      { "value": "label", "label": "Label (<small>)" },
                      { "value": "lead", "label": "Lead (<strong>)" },
                      { "value": "plain", "label": "Plain (<span>)" }
                    ]
                  },
                  "text": {
                    "type": "string",
                    "title": "Text"
                  },
                  "sup": {
                    "type": "string",
                    "title": "Superscript",
                    "description": "Optional <sup> appended after the text, e.g. price cents."
                  }
                }
              }
            },
            "style": {
              "type": "string",
              "title": "Style override",
              "localized": false,
              "description": "Extra sticker() tokens appended after the preset, e.g. \"sh:burst red\" or \"te sh:heart\". Overrides the preset for the same axis (position/hue/shape)."
            }
          }
        },
        "marquee": {
          "type": "object",
          "title": "Marquee",
          "description": "Full-width scrolling text band across the media (<ui-marquee>) — a BAND, not 9-grid furniture: it spans the frame's inline size, is positioned only top (default) or bot, and sits below the point furniture (chip/beacon/sticker). Text renders via aria-label on an empty element — no child markup.",
          "properties": {
            "text": {
              "type": "string",
              "title": "Text"
            },
            "style": {
              "type": "string",
              "title": "Style override",
              "localized": false,
              "description": "Extra marquee() tokens appended after the preset, e.g. \"red sm rpt\" or \"bot fade\". Overrides the preset for the same axis (position/hue/size/mode); unknown words are dropped by the renderer."
            }
          }
        },
        "save": {
          "type": "object",
          "title": "Save",
          "description": "Save/favorite toggle overlaid on the media (<ui-save>). The boolean value true is also accepted and defaults to a heart.",
          "properties": {
            "shape": {
              "type": "select",
              "title": "Shape",
              "localized": false,
              "default": "heart",
              "options": [
                { "value": "heart", "label": "Heart" },
                { "value": "bookmark", "label": "Bookmark" },
                { "value": "star", "label": "Star" }
              ]
            },
            "saved": {
              "type": "boolean",
              "title": "Saved",
              "description": "Initial saved state (button aria-pressed).",
              "default": false
            },
            "style": {
              "type": "string",
              "title": "Style override",
              "localized": false,
              "description": "Extra save() tokens appended after the preset, e.g. \"orange sm\" or \"non\" (hide the disc)."
            }
          }
        },
        "lightbox": {
          "type": "object",
          "title": "Lightbox",
          "description": "View-gallery / fullscreen toggle overlaid on the media (<ui-lightbox>). The renderer marks the frame as a popover and points the invoker at it (built-in toggle-popover command); the open-state presentation comes from the preset's open: tokens. The boolean value true is also accepted.",
          "properties": {
            "label": {
              "type": "string",
              "title": "Aria label",
              "localized": true,
              "default": "View gallery"
            },
            "shape": {
              "type": "select",
              "title": "Glyph",
              "localized": false,
              "default": "photos",
              "options": [
                { "value": "photos", "label": "Photo library (open gallery)" },
                { "value": "maximize", "label": "Window maximize (full screen)" }
              ]
            },
            "style": {
              "type": "string",
              "title": "Style override",
              "localized": false,
              "description": "Extra lightbox() tokens appended after the preset, e.g. \"white sm\" or \"non\" (hide the disc)."
            }
          }
        },
        "play": {
          "type": "object",
          "title": "Play",
          "description": "Play/pause affordance overlaid on the media (<ui-play>) — for videos and autoplay carousels, where it becomes the sole play/pause control. The boolean value true is also accepted.",
          "properties": {
            "label": {
              "type": "string",
              "title": "Aria label",
              "default": "Play"
            },
            "style": {
              "type": "string",
              "title": "Style override",
              "localized": false,
              "description": "Extra play() tokens appended after the preset, e.g. \"lg\" or \"bs\"."
            }
          }
        }
      }
    },
    "engagement": {
      "type": "object",
      "title": "Engagement",
      "description": "Interaction statistics — emitted as interactionStatistic → InteractionCounter (WatchAction/LikeAction/ShareAction/CommentAction)",
      "properties": {
        "viewCount": {
          "type": "number",
          "title": "Views"
        },
        "likeCount": {
          "type": "number",
          "title": "Likes"
        },
        "shareCount": {
          "type": "number",
          "title": "Shares"
        },
        "commentCount": {
          "type": "number",
          "title": "Comments"
        }
      }
    },
    "preset": {
      "type": "reference",
      "title": "Preset",
      "description": "Reference to a card-preset defining the look & feel: the host element (<ui-card> or <ui-reveal>) and its variant/media/content attribute token strings. Swap the reference to restyle the card without touching content. Falls back to the renderer's default (stack) when unset.",
      "localized": false,
      "options": {
        "referenceType": "card-preset",
        "multiple": false
      }
    },
    "flipside": {
      "type": "reference",
      "title": "Flipside",
      "description": "Optional reference to another card whose content renders as the reveal back panel. Only used when the preset's element is ui-reveal. Unset: the back panel is derived from this card's own envelope + details. The flipside shares the host's schema.org scope — use the same schemaType (or the neutral 'content') to keep microdata coherent. Resolved one level deep; a flipside's own flipside is ignored.",
      "localized": false,
      "options": {
        "referenceType": "card",
        "multiple": false
      }
    },
    "details": {
      "type": "object",
      "title": "Type Details",
      "description": "Type-specific data, discriminated by schemaType. Expected shapes — product: { price:{current,original,currency,discountText}, availability, rating:{value,count,max}, sku, validUntil, validUntilDisplay } · event: { startDate, endDate, status, location:{name,address}, organizer:{name} } · recipe: { prepTime, cookTime, servings, ingredients[], instructions[] } · review: { rating:{value,max}, reviewer:{name,verified}, reviewDate, productReviewed } · job: { company, location, employmentType, salaryRange:{min,max,currency,period}, qualifications[], benefits[], applicationDeadline } · course: { duration, courseWorkload, difficultyLevel, instructor:{name,title} (emitted as CourseInstance.instructor → Person), provider (the Organization), price:{current,original,currency}, prerequisites[] } · booking: { serviceName, venue, capacity, duration, price:{hourlyRate,currency}, amenities[], cancellationPolicy } · poll: { options:[{headline,votes}], totalVotes } · profile: { jobTitle, organization, location, bio, contacts:[{type,value,label}] } (skills are envelope tags — Person gets knowsAbout automatically) · faq: { items:[{question,answer}] } · timeline: { items:[{date,endDate,location,headline,text,theme}] } (date becomes the subEvent required startDate) · gallery: { albumName, totalCount } (categories are envelope tags) · statistic: { metricName, currentValue, unit, trend, trendPercentage } · achievement: { issuingOrganization, dateEarned, expirationDate, skillLevel, credentialId, verificationUrl } · announcement: { announcementType, priority, priorityDisplay, effectiveDate:{start,end}, targetAudience, actionRequired } · business: { businessType (allowlisted LocalBusiness subtype, e.g. Restaurant/CafeOrCoffeeShop — sharpens the emitted itemtype), address:{streetAddress,addressLocality,postalCode,addressCountry}, telephone, email, website, priceRange, rating:{value,count,max}, sameAs[], foundingDate, geo:{latitude,longitude}, openingHours:[{schema, days?, time?}] (one entry per distinct opening pattern — ranges 'Mo-We 09:00-17:00' and single days 'Th 09:00-16:00'; rendered as a two-column day/time list, days+time derived from the schema string unless given explicitly; each row also emits a structured OpeningHoursSpecification) } · comparison: { items:[{name,price,image}], recommendation, summary } · contact: { contactType, department, availableHours, availableHoursDisplay, responseTime, languages (array or comma string — one availableLanguage each), contactMethods:[{type,value,label}] } · location: { address, geo, openingHours:[{schema, days?, time?}] (or a plain hours string; a plain Place emits only openingHoursSpecification — the flat openingHours string is a LocalBusiness/CivicStructure property), contact, amenities[] } · membership: { planName, price:{monthly,yearly,currency}, trialPeriod, features[], limitations[], isPopular (renders a Most-popular chip; override the words with popularText) } · social: { platform, author, postContent, hashtags[] } · software: { version (shown as a pale-accent chip), applicationCategory, operatingSystem[], developer:{name,website}, price:{current,currency} } · organization: { foundingDate, foundingDateDisplay, numberOfEmployees, sameAs[], email, headquarters:{address:{streetAddress,postalCode,addressLocality,addressCountry}, geo:{latitude,longitude} — emitted as location → Place → geo, since Organization itself has no geo property}, offices:[{name, address:{streetAddress,addressLocality,addressCountry}, geo:{latitude,longitude}, telephone, email, openingHours:[{schema, days?, time?}]}] (offices emit department → LocalBusiness, each with its own per-day hours table) } · video: { durationDisplay (shown as a pale-accent chip), viewsDisplay, creator:{name} } (machine duration/uploadDate ride the media item; eyebrow → genre, published → uploadDate) · howto: { totalTime, estimatedCost:{value,currency}, difficulty, supplies[], tools[], steps:[{name,text}] } · qa: { question, upvotes, answers:[{text, author, upvotes, accepted}] } · podcast: { seriesName, episodeNumber, duration, durationDisplay, audioUrl } · movie: { dateReleased, dateReleasedDisplay, duration, durationDisplay, contentRating, director:{name}, actors[], rating:{value,count,max} } (eyebrow → genre) · book: { isbn, numberOfPages, bookFormat (schema.org BookFormatType member), bookFormatDisplay, publisher, rating:{value,count,max}, price:{current,currency} } · dataset: { license, licenseDisplay, temporalCoverage, spatialCoverage, variableMeasured[], distribution:[{format,url}] } · claim: { claim, claimant, reviewDate, verdict:{value,max,label} } · content/article/news/quote: no details needed (envelope only).",
      "ui": {
        "widget": "editor-card"
      }
    }
  },
  "required": []
}

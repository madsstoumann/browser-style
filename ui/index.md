# UI Components

## Input
- Auto Suggest (`auto-suggest`)
- Button (`button`)
- Button Group (`button-group`)
- Checkbox / Radio (`check-radio`)
- Checklist (`check-list`)
- Circular Range (`circular-range`)
- Color Input (`color-input`)
- Combobox (`combobox`)
- Input Button (`input-button`)
- Number (`number`)
- Number Snapper (`number-snapper`)
- Number Spinner (`number-spinner`)
- One Time Code (`onetimecode`)
- Password Strength (`password-strength`)
- Password Toggle (`password-toggle`)
- Range (`range`)
- Range Arc (`range-arc`)
- Range Circular (`range-circular`)
- Range Gauge (`range-gauge`)
- Range Mask (`range-mask`)
- Range Min/Max (`range-minmax`)
- Rating (`rating`)
- Rich Text (`rich-text`)
- Select (`select`)
- Slider (`slider`)
- Swap / Switch (`swap`)
- Tag (`tag`)
- Transfer List (`transfer-list`)
- Validate (`validate`)

## Data Display
- Avatar (`avatar`)
- Avatar Group (part of `avatar`)
- Badge (`badge`)
- Beacon (`beacon`)
- Chip (`chip`)
- Chronology (`chronology`)
- Countdown (`countdown`)
- Description List (`description-list`)
- Divider (`divider`)
- Icon (`icon`)
- List Grid (`listgrid`)
- Rating Histogram (`rating-histogram`)
- Ribbon (`ribbon`)
- Table (`table`)
- Table Expand (`table-expand`)
- Table Resize (`table-resize`)
- Timeline (`timeline`)
- Tooltip (`tooltip`)

## Feedback
- Dialog (`dialog`)
- Indeterminate (`indeterminate`)
- Message Box (`msg-box`)
- Notification (`notification`)
- Progress (`progress`)
- Progress Circular (`progress-circular`)
- Progress Goal (`progress-goal`)
- Progress Meter (`progress-meter`)
- Progress Timer (`progress-timer`)
- Skeleton (`skeleton`)
- Snack Bar (`snack-bar`)
- Toast (`toast`)

## Surfaces
- Accordion (`accordion`)
- Card (`card`)
- Card Expand (`card-expand`)
- Card Flip (`card-flip`)
- Details (`details`)
- Expand Feature (`expand-feature`)
- Feature Compare (`feature-compare`)
- Price Card (`price-card`)
- Product Card (`product-card`)
- Quote (`quote`)
- Testimonial (`testimonial`)

## Navigation
- Breadcrumbs (`breadcrumbs`)
- Carousel (`carousel`)
- Context Menu (`context-menu`)
- Dock (`dock`)
- Drawer (`drawer`)
- Footer (`footer`)
- Form Step (`form-step`)
- Launchpad (`launchpad`)
- Mega Menu (`mega-menu`)
- Menu (`menu`)
- Menu JS (`menu-js`)
- Menu List (`menu-list`)
- Nav Compass (`nav-compass`)
- Navigator Consent (`navigator-consent`)
- Overflow (`overflow`)
- Pagination (`pagination`)
- Radial Menu (`radial-menu`)
- Scroll (`scroll`)
- Scroll Mask (`scroll-mask`)
- Scroll Spy (`scroll-spy`)
- Step (`step`)
- Tab Bar (`tab-bar`)
- Tab-Cordion (`tab-cordion`)
- Tabs (`tabs`)
- Toolbar (`toolbar`)
## Layout
- Bento (`bento`)
- Grid (`grid`)
- Hex Grid (`hex-grid`)
- Bubble Grid (`bubble-grid`)
- Layout (`layout`)
- Position (`position`)
- Split Panel (`split-panel`)

## Media & Visualization
- Charts (`charts`) — area, bar, candlestick, column, donut, line, pie, poll
- Color Compare (`color-compare`)
- Color Deck (`color-deck`)
- Color Grid (`color-grid`)
- Color Key (`color-key`)
- Color Mask (`color-mask`)
- Color Palette (`color-palette`)
- Color Picker (`color-picker`)
- Color Swatch (`color-swatch`)
- Color Visualizer RGB (`color-visualizer-rgb`)
- Custom Color (`custom-color`)
- ePub Reader (`epub-reader`)
- Gallery (`gallery`)
- Hue Wheel (`hue-wheel`)
- Image Compare (`image-compare`)
- Image Gallery (`image-gallery`)
- Image Map (`image-map`)
- Image Select (`image-select`)
- Meter (`meter`)
- Slideshow (`slideshow`)
- Video Embed (`video-embed`)
- Video List (`video-list`)
- Video Scrub (`video-scrub`)
- Word Cloud (`word-cloud`)
- World Map (`worldmap`)
- XY (`xy`)

## Specialized
- Analog Clock (`analog-clock`)
- Analog Gauge (`analog-gauge`)
- Calendar (`calendar`)
- Chat (`chat`)
- Cinema (`cinema`)
- Confetti (`confetti`)
- Country (`country`)
- Dark Mode (`dark-mode`)
- Digital Clock (`digital-clock`)
- Draggable (`draggable`)
- File List (`file-list`)
- File Tree (`file-tree`)
- Flight (`flight`)
- Gradient Text (`gradient-text`)
- Journey Planner (`journey-planner`)
- Linenumber (`linenumber`)
- Location (`location`)
- Map (`map`)
- Markdown (`markdown`)
- Marquee (`marquee`)
- Mood (`mood`)
- Moon Phase (`moon-phase`)
- Piano Keys (`piano-keys`)
- Reaction (`reaction`)
- Read This (`read-this`)
- Reading Progress (`reading-progress`)
- Scroll Animations (`scroll-animations`)
- Search Bot (`search-bot`)
- Share (`share`)
- Speed Ticket (`speed-ticket`)
- Sun Phase (`sun-phase`)
- Syntax (`syntax`)
- Team (`team`)
- Text Slider (`text-slider`)
- Timeslot (`timeslot`)
- Toggletip (`toggletip`)
- Underline (`underline`)
- Weather (`weather`)
- Weather Widgets (`weather-widgets`)
- Word Replace (`word-replace`)
- Word Wheel (`word-wheel`)
- Year (`year`)

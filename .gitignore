.DS_Store
node_modules/
# ui/content-card/dist/
_tmp/
_v1/
_v2/

# local env files
.env.local
.env.*.local
.dev.vars
.wrangler/

# Editor directories and files
.idea
.vscode
.gemini-clipboard
.claude/settings.local.json
*.suo
*.ntvs*
*.njsproj
*.sln

# CMS Configuration Files
.cmsconfig
/.playwright-mcp

# Docusaurus build cache
baseline/.docusaurus/

# Demo video fixtures — harvested metadata + generated previews (posters, preview
# clips, full-length sources). Kept local on purpose: ~23 MB, signed/derived
# snapshots, and nothing but the ui/card video demos reads them.
ui/card/data/vimeo-data/
ui/card/data/youtube-data/

# transient agent worktrees
.claude/worktrees/

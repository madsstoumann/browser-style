.DS_Store
node_modules/
# ui/content-card/dist/
_tmp/
_v1/
_v2/

# local env files
.env.local
.env.*.local
.dev.vars
.wrangler/

# Editor directories and files
.idea
.vscode
.gemini-clipboard
.claude/settings.local.json
*.suo
*.ntvs*
*.njsproj
*.sln

# CMS Configuration Files
.cmsconfig
/.playwright-mcp

# Docusaurus build cache
baseline/.docusaurus/

# Vimeo harvest — client metadata + generated previews (do not commit)
ui/card/vimeo-data/
ui/card/youtube-data/

# transient agent worktrees
.claude/worktrees/

# Demo video fixtures — large binaries fetched/generated for the ui/card video
# demos (posters, preview clips, full-length sources). Kept local on purpose:
# ~23 MB, and nothing but the demo pages reads them.
ui/card/demo/vimeo-data/
ui/card/demo/youtube-data/
